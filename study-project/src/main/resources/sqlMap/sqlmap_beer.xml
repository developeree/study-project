<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study_project.web.dao.BeerDao">

	<insert id="createBeer" parameterType="Beer" useGeneratedKeys="true" keyProperty="idx">
		insert into
		beer
		(
			title,
			comment,
			price,
			edit_date,
			area,
			tumbnail,
			ad,
			company
		)
		values
		(
			#{title},
			#{comment},
			#{price},
			now(),
			#{area},
			#{tumbnail},
			#{company}
		)
	</insert>

</mapper>