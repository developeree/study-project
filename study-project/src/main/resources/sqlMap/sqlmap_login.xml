<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.study_project.web.dao.UserDao">
<!-- 	<select id="loginUser" resultType="com.study_project.web.model.User"> -->
<!--         SELECT id,password FROM user -->
<!--         WHERE id = #{id} AND password = #{password} -->
<!--     </select> -->

	
	<select id="loginUser" resultType="com.study_project.web.model.User" parameterType="com.study_project.web.model.User">
		SELECT
			*
		FROM
			user_info
		WHERE
			id = #{id}
		AND
			password = #{password}
	</select>

</mapper>