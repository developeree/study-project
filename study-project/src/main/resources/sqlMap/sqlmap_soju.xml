<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sojuSql">

	<!-- 목록 SELECT -->
	<select id="selectSojuList" resultType="com.study_project.web.soju.model.Soju" parameterType="com.study_project.web.soju.model.Soju">
		SELECT 
			idx,
			title,
			price,
			company,
			tumbnail
		FROM
			soju
		WHERE 
			1 = 1
	</select>
	
	<!-- 글쓰기 -->
	<insert id="insertSoju" parameterType="com.study_project.web.soju.model.Soju" useGeneratedKeys="true" keyProperty="idx">
		INSERT INTO soju
			(
				title,
				content,
				price,
				create_date,
				edit_date,
				area,
				tumbnail,
				ad,
				company
			)
		VALUES
			(
				#{title},
				#{content},
				#{price},
				CURRENT_TIMESTAMP,
				NOW(),
				#{area},
				#{tumbnail},
				#{ad},
				#{company}
			)
	</insert>
</mapper>